import StatusScreen from "./_components/StatusScreen";

export default function AdminLoading() {
  return (
    <StatusScreen
      title="Loading admin dashboard..."
      message="Preparing FreshCart admin tools."
    />
  );
}
